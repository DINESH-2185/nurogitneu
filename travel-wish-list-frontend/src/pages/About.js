import React from "react";

const About = () => {
  return (
    <div style={{ padding: "20px" }}>
      <h2>About This App</h2>
      <p>This is a travel wish list application built with React using in-memory static data. You can view your favorite destinations and read user reviews. This is just the frontend — no backend is connected yet.</p>
    </div>
  );
};

export default About;
